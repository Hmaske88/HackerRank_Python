import numpy as np

n=int(input())
l1=[]
l2=[]
for i in range(n):
    l1.append(list(map(int,input().split())))

for i in range(n):
    l2.append(list(map(int,input().split())))
    
a=np.array(l1)
b=np.array(l2)

print(np.dot(a,b))
