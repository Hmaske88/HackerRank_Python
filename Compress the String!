import itertools
s=input()
for key,group in itertools.groupby(s,lambda i:i[0]):
    print("("+str(len(list(group)))+","+" "+key+")",end=" ")
    # print("({}, {})".format(len(list(group)), key),end=" ")
